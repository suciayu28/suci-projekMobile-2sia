<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Suciloyalty">
        <!-- 🔹 PERTEMUAN 4 -->
        <activity android:name=".pertemuan4.Custom2Activity" />
        <activity android:name=".pertemuan4.Custom1Activity" />
        <activity android:name=".pertemuan4.RumusActivity" />
        <activity android:name=".pertemuan4.MainActivity" />

        <!-- 🔹 PERTEMUAN 3 -->
        <activity android:name=".pertemuan3.WelcomeActivity" />

        <!-- 🔥 LOGIN = HALAMAN PERTAMA -->
        <activity android:name=".pertemuan3.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

<!--        <activity-->
<!--            android:name=".MainActivity"-->
<!--            android:exported="false" />-->
    </application>

</manifest>